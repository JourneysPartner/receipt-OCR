あなたは「クレジットカード明細 自動仕訳システム」（Google Apps Script）の、**形式不明のカードを Web アプリの画面から登録する機能（§15-8）**が実装済みの作業ツリーを受け取ります。**2 回目のレビューの指摘を直してください。**

作業ツリーには実装が未コミットで残っています。いま `node test/run-tests.js` は **1180/1180 緑**、仕様書 §11 の変異 M1〜M25 はすべて赤です。**サーバーは直っています。残りはほぼ画面（`src/81_WebAppUi.html`）です。**

## 仕様書

`work/spec_format_registration.md` は **1.2** に改めました。**冒頭の改訂履歴 1.2 を読み、次を必ず読んでから直してください**：§5.6 の戻り値の説明（`excluded` と `excludedBeforeStart`）・§6.1 の `ACK_CUSTOMER_SIDE_REQUIRED`・§6.2 の `EXCLUDED_ROWS` と `PURPOSE_HEADER_NOT_STANDARD`・**§7.11 の U2（書き直した）と U13〜U23**・§10.8・§11 の M26〜M28。

**§7.11 U2 は 1.1 の書き方が誤っていました。**「空白除去」と書きましたが、サーバーの `normalizeMerchant`（`src/32_MerchantNormalizer.gs`）は空白と改行を半角空白 1 つにまとめて端を削るだけで、消しません。1.1 のとおりに実装された結果、「使用 用途」のような見出しでサーバーは確認を求め、画面はチェック欄を出さず、保存できないままになります。**`normalizeMerchant` と同じ処理を画面に 1 つの関数として置いてください。**

仕様書に書かれていることが正です。書かれていないことは自分で決めてよいが、決めた箇所は報告してください。

---

## 絶対に守ること（前回と同じ）

1. **`clasp` のどのコマンドも実行しないこと。**
2. **git を操作しないこと**（commit・stash・reset・checkout・clean）。差分は作業ツリーに残したまま止めること。
3. **`src/` の差分は同じ 8 ファイルだけ**（`13`・`97`・`04`・`52`・`03`・`19`・`80`・`81`）。`70`・`71`・`24`・`30`・`31`・`32`・`35`・`43`・`44`・`45`・`51`・`96`・`06`・`00` に 1 行の差分も作らないこと。検査：`git status --porcelain -- src/` がちょうど 8 行。
4. テストの変更は `test/phase9-format-registration.test.js` だけ（と、要るなら `test/gas-stubs.js`。変えたら報告）。`test/fixtures/` と他の既存テストは 1 文字も変えないこと。
5. **テストを先に赤にしてから直すこと。**
6. 既存の 1180 本を 1 つも壊さないこと。
7. リポジトリの親フォルダの `.codex/config.toml` に触らないこと。

---

## 直すこと

### サーバー（`src/19_FormatRegistration.gs`）

- **S-1** 試し読みの `extraction.excluded` から `_rowRange` の行を外し、件数を `excludedBeforeStart` として返す。`EXCLUDED_ROWS` は開始行より後の除外だけを数える。**読み戻しの比較（`formatExtractionSignature_`）は `_rowRange` を含む全部で比べること**（§5.6。表示を絞っても比べる材料は絞らない）。fmt 80。
- **S-2** 用途が「なし」のときは、`PURPOSE_HEADER_NOT_STANDARD` と、見出しを理由とする `ACK_CUSTOMER_SIDE_REQUIRED` を出さない（`PURPOSE_COLUMN_REQUIRED` が止める）。診断の `customerSide` による `ACK_CUSTOMER_SIDE_REQUIRED` は残す。fmt 81。

### 画面（`src/81_WebAppUi.html`）── 仕様書 §7.11

中の 4 つを先に：

- **U2（書き直し）** 見出しの正規化を `normalizeMerchant` と同じにする。いまの実装は 1654 行と 1827 行付近の 2 か所で `.replace(/\s/g, '')` を使っている。1 つの関数にまとめる。fmt 79・M26。
- **U14** 入力の変更でパネル全体を作り直さない（いまは `formatUpdateAnswer` 1710〜1714 行付近が `renderFormatPanel()` を呼ぶ）。**押されたボタンやフォーカスのある欄を DOM から外さないこと。**fmt 82・M27。
- **U15** 取込待ちへ戻した後に `webAppExplainUnknownFile` を呼び直さない（いまは 2172〜2185 行付近）。fmt 83・M28。
- **U16** 除外した行の表に見出しを付け、理由を日本語にする。fmt 84。

残り（U13 の続き・U17〜U23）もすべて直すこと。行番号の目安（いまの `81`）：U13 1659〜1670・1808〜1816／U17 1553〜1571・2023〜2027／U18 1578〜1579／U19 2065〜2070・2257〜2263／U20 1636〜1641・2119〜2122／U21 1931〜1940・1951・`renderSelection`・1019・1238／U22 2134〜2145・1161〜1196・2221／U23 1975〜2042・1823〜1840。止める理由・警告を `<p class="tag …">` で出している所（136・1826・1868・1909 付近）は、`.tag` が inline-block の丸い札なので長い文が崩れる ── block 表示の専用クラスにすること（U6 の続き）。

**テスト**：§10.8 の fmt 79〜85。クライアントの判断（正規化・応答の採否・文の組立て・どの関数を呼ぶか）は関数に切り出し、`test/phase8-webapp.test.js` の `clientEval` と同じ手で取り出して確かめること。描画の細部（色・配置）はテストしなくてよい。

---

## 進め方

1. 仕様書 1.2 の変わった節を読む
2. §10.8 のケースを書く → 赤になることを確かめる
3. S-1・S-2 → U2・U14・U15・U16 → 残り、の順に直す
4. `node test/run-tests.js` を全部緑にする
5. §11 の M1〜M28 を回す
6. `git status --porcelain -- src/` が 8 行であることを確かめる

## 報告してほしいこと

- `node test/run-tests.js` の最後の行
- 仕様書 1.2 が間違っていた箇所（黙って直さないこと）
- 仕様書に書いていなくて自分で決めた箇所
- 赤にならなかった変異と、その理由の見立て
- `test/gas-stubs.js` を変えたなら何を・なぜ
- 足したテストの数

**レビューはあなたが行いません。**終わったら上を書いて止めてください。
