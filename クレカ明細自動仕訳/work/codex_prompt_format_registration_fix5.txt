あなたは「クレジットカード明細 自動仕訳システム」（Google Apps Script）の、**形式不明のカードを Web アプリの画面から登録する機能（§15-8）**が実装済みの作業ツリーを受け取ります。**5 回目のレビューの指摘を 1 つだけ直してください。**

作業ツリーには実装が未コミットで残っています。いま `node test/run-tests.js` は **1214/1214 緑**、仕様書 §11 の変異 M1〜M37 はすべて赤です。

## 指摘

前回の実装で、保存の後の処理が例外になったときに `readback: 'UNCERTAIN'` を返す形が足されました（`src/19_FormatRegistration.gs` の `formatSave_`。`installCardFormat` の直後から始まる `try`）。**そのとき形式を有効のまま残しています。**テストもそれを固定しています（`test/phase9-format-registration.test.js` 1625〜1641 行付近、登録の監査で例外を起こして `enabled === true` を確かめているケース）。

これは仕様書の P4（書いた値を信じない。保存した行から読み直して一致しなければ無効にする）に反します。登録の監査や読み戻しの読取で例外が出た場合、**読み戻しの確認を一度も終えていない形式が、全顧客の取込に効きます。**

## 仕様書

`work/spec_format_registration.md` は **1.5** に改めました。**改訂履歴 1.5、§5.7 の手順 7'、§7.11 の U41、§10.11、§11 の M38・M39 を読んでから直してください。**

要点：

- **確認（行の値・結果・判定）がすべて一致する前の例外** → `disableCardFormat_(formatId, 'READBACK_UNCERTAIN', 実行者)` を試み（これ自体の失敗も例外から守る）、`{saved: true, formatId, version: 1, readback: 'UNCERTAIN', disabled: 無効にできたか}` を返す。
- **確認がすべて一致した後の例外**（兄弟ファイルの列挙など）→ 形式は無効にせず `readback: 'OK'`、`siblings` を空、`siblingsError: true`。
- 画面（U41）：`UNCERTAIN`＋`disabled: true` なら「安全のため無効にしました」と言い、取消しのボタンは出さない。`disabled: false` のときだけ取消しのボタンを出す。`siblingsError: true` なら「同じフォルダのファイルを確かめられませんでした」と言う。

---

## 絶対に守ること（前回と同じ）

1. **`clasp` のどのコマンドも実行しないこと。**
2. **git を操作しないこと**（commit・stash・reset・checkout・clean）。
3. **`src/` の差分は同じ 8 ファイルだけ。**検査：`git status --porcelain -- src/` がちょうど 8 行。
4. テストの変更は `test/phase9-format-registration.test.js` だけ。`test/fixtures/` と他の既存テストは 1 文字も変えないこと。
5. **テストを先に赤にしてから直すこと。**既存の `UNCERTAIN` のケースは fmt 113 の期待（`disabled: true`・D＝FALSE）に書き換え、いまの実装で赤になることを確かめてから直す。
6. 既存の 1214 本を 1 つも壊さないこと。
7. リポジトリの親フォルダの `.codex/config.toml` に触らないこと。
8. **この 1 件以外は変えないこと**（§14 K-F6 の低 10 件も触らない）。

## 進め方

1. 仕様書 1.5 の該当節を読む
2. fmt 113〜116 を書く（既存の `UNCERTAIN` のケースを書き換える）→ 赤を確かめる
3. `formatSave_` と画面を直す
4. `node test/run-tests.js` を全部緑にする
5. §11 の M1〜M39 を回す
6. `git status --porcelain -- src/` が 8 行であることを確かめる

## 報告してほしいこと

- `node test/run-tests.js` の最後の行と、テストの本数の増減
- 仕様書 1.5 が間違っていた箇所（黙って直さないこと）
- 自分で決めた箇所
- 赤にならなかった変異と、その理由の見立て

**レビューはあなたが行いません。**終わったら上を書いて止めてください。
