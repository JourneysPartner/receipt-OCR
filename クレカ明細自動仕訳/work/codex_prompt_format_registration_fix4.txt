あなたは「クレジットカード明細 自動仕訳システム」（Google Apps Script）の、**形式不明のカードを Web アプリの画面から登録する機能（§15-8）**が実装済みの作業ツリーを受け取ります。**4 回目のレビューの指摘を直してください。範囲は絞ってあります ── 中の 5 つ（U36〜U40）と、それを捕まえるテスト（fmt 106〜112）だけです。**

作業ツリーには実装が未コミットで残っています。いま `node test/run-tests.js` は **1207/1207 緑**、仕様書 §11 の変異 M1〜M32 はすべて赤です。高はもう残っていません。

## 仕様書

`work/spec_format_registration.md` は **1.4** に改めました。**冒頭の改訂履歴 1.4、§7.11 の U36〜U40、§10.10、§11 の M33〜M37、付録 B の `REQUEUE_FAILED` の行を必ず読んでから直してください。**§14 K-F6 に挙げた低 10 件は**今回は直さないでください**（範囲を広げると、また新しい欠陥を持ち込む ── この画面では毎回そうなった）。

仕様書に書かれていることが正です。書かれていないことは自分で決めてよいが、決めた箇所は報告してください。

---

## 絶対に守ること（前回と同じ）

1. **`clasp` のどのコマンドも実行しないこと。**
2. **git を操作しないこと**（commit・stash・reset・checkout・clean）。差分は作業ツリーに残したまま止めること。
3. **`src/` の差分は同じ 8 ファイルだけ**（`13`・`97`・`04`・`52`・`03`・`19`・`80`・`81`）。検査：`git status --porcelain -- src/` がちょうど 8 行。
4. テストの変更は `test/phase9-format-registration.test.js` だけ。`test/gas-stubs.js` は要るなら変えてよいが報告すること。`test/fixtures/` と他の既存テストは 1 文字も変えないこと。
5. **テストを先に赤にしてから直すこと。**
6. 既存の 1207 本を 1 つも壊さないこと。
7. リポジトリの親フォルダの `.codex/config.toml` に触らないこと。

---

## 直すこと

**先にテストの道具を足してください**（仕様書 §10.10 の冒頭）：

- 偽の `google.script.run` に、**応答を保留してテストが後で返す**形（いまは同期で即答するので、処理中の状態や遅れた応答を作れない）
- 偽の DOM に、**フォーカス**（`document.activeElement`・`focus()`・`blur()`。フォーカスが外れたら、値が変わっていれば `change` を起こす）

そのうえで：

- **U36** 取込・確定の実行中も形式パネルを描き直し、処理中は入力を無効にする。画面の値と `state.formatAnswers` を食い違わせない（いまは `startImport` 1026〜1034・1077〜1083 行付近が `updateActionButtons` だけを呼び、`formatUpdateAnswer` が処理中の入力を黙って捨てる）。fmt 106・M33。**`fmt 91` はテスト自身が `renderSelection()` を呼んでいるのでこの欠陥を隠している ── `startImport` を実際に動かし、応答を保留して確かめること。**
- **U37** 保存が不確かなまま診断し直して `MATCHES_ACTIVE`（保存しようとした形式 ID と同じ）なら［この登録を取り消す］も出す（2339〜2349・2162〜2167 行付近）。サーバーは `installCardFormat` の後の処理（`19` 740〜792 行付近）で例外が出ても、取り消せる形にする。fmt 107・M34。
- **U38** 確認のチェック欄を、中身が変わったときだけ作り直す（1783〜1786・1860〜1861 行付近）。fmt 108・M35。
- **U39** 保存した後は［顧客に修正を依頼する］を出さない。`returnFormatToCustomer` も断る（2200〜2203・2403〜2417 行付近）。fmt 109・M36。
- **U40** サーバーの `webAppRequeueFormatFiles`（`80` 90〜112 行付近）を 1 件ずつ例外から守り、失敗は `skipped`（`REQUEUE_FAILED`）。画面は例外でもパネルの中に文を出し、一覧と要確認を読み直す（2399 行付近）。fmt 110・fmt 111・M37。
- **fmt 112** `saveFormat`・`previewFormat`・`returnFormatToCustomer` を `ok: false` で偽の DOM の上に通し、固まらないことを確かめる（いまの fmt 74・16d は補助関数だけの検査）。

---

## 進め方

1. 仕様書 1.4 の変わった節を読む
2. テストの道具（保留する偽物・フォーカス）を足す
3. fmt 106〜112 を書く → 赤になることを確かめる
4. U36〜U40 を直す
5. `node test/run-tests.js` を全部緑にする
6. §11 の M1〜M37 を回す
7. `git status --porcelain -- src/` が 8 行であることを確かめる

## 報告してほしいこと

- `node test/run-tests.js` の最後の行と、テストの本数の増減
- 仕様書 1.4 が間違っていた箇所（黙って直さないこと）
- 仕様書に書いていなくて自分で決めた箇所
- 赤にならなかった変異と、その理由の見立て
- `test/gas-stubs.js` を変えたなら何を・なぜ

**レビューはあなたが行いません。**終わったら上を書いて止めてください。
