報告ありがとうございます。**3件とも本物で、すべて仕様書の側が誤っていました。**同じ型の欠陥（仕様書が、取込側の書かない値を出せと要求する）はこれで3度目なので、**継ぎ接ぎをやめて規則を一般化しました**。仕様書は版 2.16 です。

## 仕様書に入れた新しい規則（§7.4）

識別子は `live` が無いとき、次を**この順に連ね、空の項は飛ばす**：

`merchantOriginal`（O列。空なら `（店名なし）`）／`fileNameOriginal`（J列）／`sourceRow`（L列）／`displayTxId`（F列）── **最後に `displayTxId` が空なら `fullTxId`（E列）を出す。これは必ずある。**

種別ごとに何が埋まるかの実測表も入れました（あなたの調査結果です）：

| 種別 | `merchantOriginal` | `sourceRow` | `displayTxId` | `fullTxId` |
|---|---|---|---|---|
| `PARTNER` | ○ | ○ | ○ | ○ |
| `DATE`・`AMOUNT`・`ZERO_AMOUNT` | × | ○ | × | ○ |
| `PRIOR_YEAR` | × | × | × | ○ |

取込側に項目を揃える案は K-M14 として第2段へ送りました（`70`・`35`・`30` は §11.1 の「触らない」一覧なので範囲外）。

## 作業（4件。いずれも小さい）

### 1. `fullTxId` のフォールバックを足す

`menuReviewStoredIdentity_`（`src/96_Menu.gs:1102`）に、`displayTxId` が空なら `fullTxId` を出す分岐を足してください。**`PRIOR_YEAR` は店名も行番号も無いので、これが唯一の識別手段です。**現状は `（店名なし） / ファイル名` で止まり、同じファイルに複数あると区別できません。

### 2. `matchMethod` の表示を落とす

`src/96_Menu.gs:1133` が値があれば `matchMethod` を表示しますが、仕様書 §7.3 は「出そうとしてはならない」に変わりました。`merchantCandidates_`（`33_MerchantMatcher.gs:98-106`）が `matchMethod` を持たず、`70_ImportFlow.gs:107` が `|| null` で写すので実データでは常に `null` です。**潜在的な差でも、正本が2つある状態は残さないでください。**

### 3. 赤にするテストを足す

- `PRIOR_YEAR` の要確認を `runImport` で立て、結果画面の識別子に**取引IDが出る**こと（店名・行番号が無くても一意に区別できること）
- `DATE` の要確認で、識別子が `（店名なし） / ファイル名 N 行目` になること（店名が無い種別の形を固定する）

### 4. 確認

**同じ型の欠陥がまだ残っていないか、もう一度だけ実データで確かめてください。**画面に出るすべての項目について、`runImport` で立てた各種別（`PARTNER`・`DATE`・`AMOUNT`・`ZERO_AMOUNT`・`PRIOR_YEAR`・ファイル単位）で実際に値が入るかを見て、空になるものがあれば報告してください。3度出ている以上、4度目を疑うのが妥当です。

## 絶対に守ること

- **`clasp push` を実行しないこと。**
- **`src/` の差分は `96_Menu.gs` と `97_Ops.gs` の2ファイルだけ。**
- テストを先に赤にしてから直すこと。既存942件を壊さないこと。
- テストの材料は `runImport` で立てること。

## 報告してほしいこと

1. どう直したか（ファイル:行）
2. 赤→緑の確認結果とテスト通過数
3. 作業4の結果（空になる項目の一覧。無ければ「無い」と、何をどう確かめたかを添えて）
