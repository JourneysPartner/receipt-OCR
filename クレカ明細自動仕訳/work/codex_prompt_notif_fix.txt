先ほどあなたが実装したメール通知のレビュー結果です。2点直してください。

レビュアーは12種の変異を「バックアップ→置換→実行→復元」で流して検証しています（全件復元確認済み）。指摘は実測に基づくものです。

## 前提

- 仕様書：`work/spec_notifications.md`・`work/spec_menu_readonly.md`（正）
- 現在 810/810 通過。**作業後も全件通ること。**
- `clasp push` はしないでください。

## 指摘1（必須）：`errorTotal` が2箇所で別計算になっており、テストが守っていない

`src/95_Notifications.gs:209`（記憶側）と `src/97_Ops.gs:587`（判定側）が、同じ意味の合計を別々に計算しています。

**209行から `+ Number(item.errorDropped || 0)` を削除しても810/810が通ります。**（画面側587行を同様に壊すと809/810になり、そちらは守られています。）

両式は現在一致しており動作は正しいのですが、片方が回帰してもテストが気づきません。

**回帰したときに何が起きるか**：W列の切詰め開始後（200件超＝10分間隔で約33時間）、`watch`＝199 に対し `errorTotal`≥200 が恒常化し、**新しい失敗が無くても評価のたびに `REPEATED_FAILURE` が立ちます**。画面には毎回表示され `ERROR_RECORDS` を covered で隠します。メールは6時間窓で抑えられますが、`lastSeenAt` が毎回更新されてエピソードが失効しないため、そのファイルが `DISCOVERED` を離れるまで6時間ごとに要対応が届き続けます。

仕様書§5.1が最も避けようとした「**直した直後に鳴る警報**」そのものになります。

### 直し方

`src/97_Ops.gs` に `fileErrorTotal_(file)` を1つ置き、**両方からそれを呼んでください**（または `assessImportStatus_` が `errorTotalByFileId` を返し、95側は計算しない形でも構いません）。同じ意味の値を2箇所で計算する構造をやめることが目的です。

### 追加するテスト

`errorCount: 199, errorDropped: 5` のファイルを `watch: {}` で通し、`watchByBucket.C001.fileA === 204` を検証。続けて同値で再評価して**鳴らないこと**を検証してください。

レビュアーはこのテスト案が、209行の変異で落ち、無変異で通ることを実証済みです。

## 指摘2（必須）：通知の失敗が取込を止めない保証に穴がある

`src/95_Notifications.gs:373-376` の `notifyImportState_` の外側 catch を `throw` に変えても810/810が通ります。

内側 try の外にある `readNotificationState_`（337行）が `PropertiesService` 障害で落ちると**tick全体が止まります**。その回帰をテストが検知できません。

仕様書§11.1 の固定に、`getScriptProperties` を投げるスタブを使い、`scheduledImportTick` が `doesNotThrow` かつ `runImport` が呼ばれることを検証するテストを追加してください。

## 指摘3（任意）

`src/95_Notifications.gs:116` の期限切れ→新規扱いを外しても通過します。影響は「新規/継続中」表示と `seenCount` だけ（規則3でdueにはなる）で軽微です。テストを足すかどうかは判断に委ねます。

## 妥当と判定された点（直さないでください）

- あなたが報告した仕様書との差異3件（テスト33b・33d・§7.7/§13.3の順序）は**いずれも妥当**と判定されました
- 事故検知テスト（1・1b・2・2b・2c）は自明通過ではないことが変異で確認されました
- 洪水対策（6時間窓・3通上限・GC・失敗時の`lastSentAt`）はすべて変異で落ちることを確認
- ロック内I/Oは `getProperties`1・`getRemainingDailyQuota`≤3・`sendEmail`≤3・`setProperty` のみで、20秒に余裕あり
- 明細漏れなし。SYSTEMバケットで `fileId` が出ないことも変異で確認
- 既存 `ops*` の戻り値・Logger出力・`scheduledImportTick` の既存ログはHEADと同一

## 報告してほしいこと

- 変更したファイルとテストの通過数
- 指摘1で共有ヘルパーに寄せた後、**209行相当を壊すと新テストが落ちること**を確認した結果
- 指摘2のテストが、外側catchを `throw` に変えると落ちることを確認した結果
- 指摘3をどう判断したか
