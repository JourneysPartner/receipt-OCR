あなたは「クレジットカード明細 自動仕訳システム」（Google Apps Script）の、**形式不明のカードを Web アプリの画面から登録する機能（§15-8）**が実装済みの作業ツリーを受け取ります。**3 回目のレビューの指摘を直してください。**

作業ツリーには実装が未コミットで残っています。いま `node test/run-tests.js` は **1195/1195 緑**、仕様書 §11 の変異 M1〜M28 はすべて赤です。サーバーはほぼ直っています。**残りは画面（`src/81_WebAppUi.html`）と、画面のテストの確かめ方です。**

## いちばん大事なこと

**画面に、列の選択を変えるたびに例外が出る欠陥があります。**U23 の「手で選択」のために足した `state.formatManualColumns` がどこでも初期化されておらず（`state` の初期値にも、パネルを開く・閉じる処理にも無い）、`state.formatManualColumns[role] = true`（1833 行付近）と `state.formatManualColumns.amount = true`（1880 行付近）で `TypeError` になります。落ちるので、その後の「試し読みを古くする」「保存を押せなくする」が走らず、画面は古い列の結果を今の結果として見せ続けます。

**テストがこれを捕まえなかったのは、画面のテストの一部が関数のソースに文字列が含まれるか（`String(fn).includes(…)`）を見ていたからです。**`test/phase9-format-registration.test.js` にこの形が 14 か所あります。文字列があることと、動くことは別です。

**仕様書 §9 に「画面のテストは振る舞いで確かめる」を足しました。**リポジトリに npm の依存は無いので、**テストの中に小さな偽の DOM を書いてください**（要るものだけ：`createElement`・`appendChild`・`removeChild`・`textContent`・`value`・`checked`・`disabled`・`addEventListener`・イベントの発火・`getElementById`・`querySelector`）。`81_WebAppUi.html` の `<script>` をその上で動かし、`google.script.run` は記録つきの偽物にして（呼ばれた関数と引数を残し、決めた値を返す）、**パネルを実際に描き、欄を変え、ボタンを押して**確かめてください。**14 か所のソース文字列の検査は、振る舞いの確かめ方に置き換えること**（fmt 102）。

## 仕様書

`work/spec_format_registration.md` は **1.3** に改めました。**冒頭の改訂履歴 1.3、§9 の最後の項、§6.2 の `REVIEW_ROWS`、§7.11 の U24〜U35、§10.9、§11 の M29〜M32 を必ず読んでから直してください。**

仕様書に書かれていることが正です。書かれていないことは自分で決めてよいが、決めた箇所は報告してください。

---

## 絶対に守ること（前回と同じ）

1. **`clasp` のどのコマンドも実行しないこと。**
2. **git を操作しないこと**（commit・stash・reset・checkout・clean）。差分は作業ツリーに残したまま止めること。
3. **`src/` の差分は同じ 8 ファイルだけ**（`13`・`97`・`04`・`52`・`03`・`19`・`80`・`81`）。ほかの `src/` のファイルに 1 行の差分も作らないこと。検査：`git status --porcelain -- src/` がちょうど 8 行。
4. テストの変更は `test/phase9-format-registration.test.js` だけ（偽の DOM もこのファイルの中に置く。ほかのファイルを増やさない）。`test/gas-stubs.js` は要るなら変えてよいが報告すること。`test/fixtures/` と他の既存テストは 1 文字も変えないこと。
5. **テストを先に赤にしてから直すこと。**とくに fmt 94 は、いまの実装で**例外が出て赤になる**ことを確かめてから直すこと。
6. 既存の 1195 本を 1 つも壊さないこと（ソース文字列の検査を置き換えた結果、テストの本数が変わるのは構わない。報告すること）。
7. リポジトリの親フォルダの `.codex/config.toml` に触らないこと。

---

## 直すこと

### 画面（`src/81_WebAppUi.html`）── 仕様書 §7.11 U24〜U35

- **U24【高】** `state` の初期値を揃える。欄の変更の処理を例外で落とさない。fmt 94・M29。
- **U25** 一覧に足す `REFERENCE` の金額列は提案の出どころで決める（1715〜1726・1869〜1899 付近）。fmt 95・M30。
- **U26** パネル全体の作り直しを、開く・処理中の状態が変わる・保存の状態が変わるときだけにする。いまは `loadFinalReview` の終わり（1507 付近・1493 付近）が `renderSelection` → `renderFormatPanel` で作り直し、入力中の欄のフォーカスを外す。fmt 96。
- **U27** 見出し行・開始行・列の変更で、見本の表の役割名と強調（1777〜1800 付近）・選択肢の見出しの文言（1822〜1826 付近）も描き直す。金額候補の合計・相違（1884〜1896 付近）は今の試し読みがあるときだけ。
- **U28** 取込待ちへ戻した後の残りの表示に、戻し終えたファイルの行を出さない（2024〜2039 付近）。fmt 100。
- **U29** 保存の結果があるうちは、フォルダ・顧客の切り替え（`chooseFolder` 897〜905 付近・`invalidateOperations` 838〜848 付近）でも確認を挟む。fmt 99。
- **U30（画面）** 保存の呼出しが例外で終わったら「保存できたか確かめられません」をパネルの中に出し、試し読みを古くして診断し直す（2239 付近の catch）。fmt 97。
- **U31** パネルを開いたら `scrollIntoView`。形式に関する文はパネルの中に出す（2170〜2195・2200・2230・2268 付近）。
- **U32** `formatSetBusy`（2149〜2161 付近）が描画の例外で固まらないようにする。診断の失敗で空のパネルを残さない（2193 付近）。［閉じる］（2358 付近）も処理中は無効に。
- **U33・U34・U35** 仕様書のとおり（1593・1818・2115 付近）。

### サーバー（`src/19_FormatRegistration.gs`）

- **U30（サーバー）** 保存後の兄弟ファイルの読取と判定（793〜808 行付近）を 1 件ずつ例外から守り、失敗したものを `siblingsNotRead` に入れる。fmt 98・M31。
- **`REVIEW_ROWS` の件数**（660 行付近）を `classifyValidationResult` の `reviewEntries` の数にする。fmt 101・M32。

---

## 進め方

1. 仕様書 1.3 の変わった節を読む
2. 偽の DOM をテストの中に書く
3. §10.9 のケースを書く → 赤になることを確かめる（fmt 94 は例外で赤）
4. 14 か所のソース文字列の検査を、偽の DOM の上での振る舞いの確かめ方に置き換える（置き換えた後も、元の検査が守っていた変異 M1〜M28 が赤になること）
5. U24 → U25〜U35 → サーバーの 2 つの順に直す
6. `node test/run-tests.js` を全部緑にする
7. §11 の M1〜M32 を回す
8. `git status --porcelain -- src/` が 8 行であることを確かめる

## 報告してほしいこと

- `node test/run-tests.js` の最後の行と、テストの本数の増減
- 置き換えたソース文字列の検査の数と、置き換え先
- 仕様書 1.3 が間違っていた箇所（黙って直さないこと）
- 仕様書に書いていなくて自分で決めた箇所
- 赤にならなかった変異と、その理由の見立て
- `test/gas-stubs.js` を変えたなら何を・なぜ

**レビューはあなたが行いません。**終わったら上を書いて止めてください。
