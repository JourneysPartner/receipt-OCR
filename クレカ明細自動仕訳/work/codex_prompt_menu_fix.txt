先ほどあなたが実装した参照系カスタムメニューのレビュー結果が出ました。2点直してください。

レビュアーは実装を8箇所わざと壊してテストの反応を確かめています。指摘は実測に基づくものです。

## 前提

- 仕様書：`work/spec_menu_readonly.md`（正）
- 対象：`src/96_Menu.gs`、`src/97_Ops.gs`、`test/phase7-menu-readonly.test.js`
- 現在 764/764 通過。**作業後も全件通ること。**
- `clasp push` はしないでください。

## 指摘1（必須）：テストが壊れても落ちない箇所が4つある

次の4箇所を壊しても 764/764 が通ってしまいました。**src の実装自体は正しく動いています。**問題はテストが守れていないことです。

| 壊した箇所 | 落ちるべきテスト | 実際 |
|---|---|---|
| `96_Menu.gs:543` `buildLeaseRows_` の顧客フィルタを外す | テスト4 | 通過 |
| `96_Menu.gs:363` `buildImportStatusView_` の顧客フィルタを外す | 何か | 通過 |
| `96_Menu.gs:556-557` 検出／解放の可・不可を逆にする | テスト25 | 通過 |
| `96_Menu.gs:405` 「30分以上動きなし」条件を外し常に警告 | テスト9/10 | 通過 |

原因と直し方（レビュアーの指摘そのまま）：

- **テスト4**（`test/phase7-menu-readonly.test.js:206`）はリース画面のファイル名を処理ログから取るが、`processRow` の既定名が `f2.csv` で、検証は `c2.csv` しか見ていない。**他顧客の漏洩を検出できない。**
  → `assert.doesNotMatch(html, /f2\.csv/)` を足す。

- **「取込の状況」を他顧客混在データで呼ぶテストが無い**（テスト9〜13はすべて `C001` のみ）。
  → `menuShowImportStatus` を `C002` 混在で呼び、件数が固定されることを検証する。

- **テスト25** は `collectLeaseStatus_` と `opsShowLeases` を比較しているが、改修後の `opsShowLeases` は `collectLeaseStatus_` を呼ぶだけ（`97_Ops.gs:1195`）なので**自分自身との比較**になっている。仕様§10.3-25 が求める `menuShowLeases` 側の表示が一度も検証されていない。
  → `menuShowLeases` の HTML の 可・不可 を `opsShowLeases` の行と突き合わせる。

- **30分閾値を跨ぐケースが無い**（テスト9は40分前、テスト10は `DISCOVERED` なし）。
  → `DISCOVERED` 1件かつ最終活動10分前で、警告が**出ない**ケースを足す。

**src は変えないでください。**テストだけの補強です。
補強したら、上の4箇所を実際に壊して**落ちることを確認**し、必ず元に戻してください。

なお、テスト12・34・14・9（警告の和集合）・30 は壊すと落ちることが確認済みです。これらは触らないでください。

## 指摘2（必須）：非列挙 `customerId` をやめ、`collect*_` の抽出に置き換える

`src/97_Ops.gs:1142` の `Object.defineProperty(..., {enumerable: false})` を廃止してください。

理由：
- **仕様書はこの設計を指示していません。**あなたの発明です。仕様§7.8・§9.4 が「`detail[]` に `customerId` を追加」と「Logger出力を一字一句変えない」を同時に書いており矛盾しているのが元凶ですが、解決策として非列挙プロパティは選ばれていません。
- **消えたときに無音で誤答します。** `filterByScope_`（`96_Menu.gs:683`）は空の `customerId` を非オーナーに非表示とするため、確認担当者には「対象はありません（すべて現行の規則どおりです）」という**偽の安心表示**が出ます。`Object.assign`・キャッシュ経由・`plain()` を挟むだけで発現します。
- このシステムは直近、「黙って空回りする」不具合が連続して問題になりました。無音で誤答する設計は入れられません。

直し方（レビュアーの推奨）：
他の2つと同じく `collectTagBackfillTargets_()` を抽出し、`opsFilesNeedingTagBackfill` はそこから従来のJSONを組み立てる形にしてください。
`customerId` は `collect*_` の戻り値には**普通の列挙可能プロパティとして**持たせ、`opsFilesNeedingTagBackfill` が組み立てる際に落とします。

**`opsFilesNeedingTagBackfill` の戻り値とLogger出力は従来どおり一字も変えないこと。**テスト30がそれを検証しています（改修前のコードでも通ることが確認済みです）。

## 報告してほしいこと

- 変更したファイルと、テストの通過数
- 指摘1で補強した4テストについて、**壊したら落ちることを確認した結果**
- 指摘2の置き換え後、テスト30が通ることの確認
- 仕様書に矛盾を見つけた場合（§7.8 と §9.4 のように）は、勝手に直さず報告してください
