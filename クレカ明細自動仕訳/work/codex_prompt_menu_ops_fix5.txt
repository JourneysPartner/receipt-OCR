4例目の報告、的確でした。**一覧が `menuReviewStoredIdentity_` を使っていない**という指摘が最も重要です ── 担当者が番号を選ぶのはその画面なので、そこで区別できなければ結果画面で区別できても遅い。2件直してください。

## 作業1【高】最初の対象一覧で、取引単位の行を区別できるようにする

`src/96_Menu.gs:600-603` の `item.kind === 'TRANSACTION'` の枝が、`review.originalDate` と `review.originalAmount` を直接読んでいます。**どちらも取込側が一度も書かない**ので、`DATE`・`AMOUNT`・`ZERO_AMOUNT`・`PRIOR_YEAR` の行は実データでこうなります：

```
 3) 金額0 | 顧客一 | 202601.csv | （店名なし） | (不明) / -
 4) 金額0 | 顧客一 | 202601.csv | （店名なし） | (不明) / -
```

**同じファイルに複数あると、選ぶ時点で区別できません。**

**直し方**：この枝を `menuReviewStoredIdentity_` と同じ規則（§7.4 の表）に揃えてください ── 店名／ファイル名／行番号／取引ID をこの順に連ね、空の項は飛ばし、`displayTxId` が空なら `fullTxId`。

一覧は `live` を持てません（15件 × 4往復 ＝ 60往復で予算を壊すため）。**取引を読みに行かないこと。**

`PARTNER_GROUP` の枝（599行の「例:」）も同じ関数を通るので、あわせて揃います。

## 作業2【中】操作選択・確認画面の店名を `live` から採る

`src/96_Menu.gs:1090` 付近が、`live` があるときも店名だけは `review.merchantOriginal` を読んでいます。`PARTNER` 以外では空です。

あなたの報告どおり、**取引ログには実値があります**（`originalMerchant` ／ `planned.k`）。`live` があるときは**そちらから採ってください**。利用日・金額・転記行と同じ扱いにするだけです。

`DATE` の `planned.b`（利用日）と `AMOUNT` の `planned.m`（金額）が空なのは**要確認の理由そのもの**なので、そのままで正しい ── あなたの判断どおりです。

## テスト

材料は `runImport` で立てること。最低限：

1. 同じファイルに `PRIOR_YEAR` が2件あるとき、**一覧の2行が異なる文字列**になること
2. `DATE` の操作選択画面に、取引ログの**実際の店名**が出ること

## 絶対に守ること

- **`clasp push` を実行しないこと。**
- **`src/` の差分は `96_Menu.gs` と `97_Ops.gs` の2ファイルだけ。**
- テストを先に赤にしてから直すこと。既存945件を壊さないこと。

## 報告してほしいこと

1. どう直したか（ファイル:行）
2. 赤→緑の確認結果とテスト通過数
3. **5例目**があれば報告してください。なければ「無い」と、何をどう確かめたかを添えて。

なお、あなたが報告した3つ目（`96_Menu.gs:1368` の要確認一覧が `- / -` になる件）は**既存の参照系メニューの欠陥**で、`gas-push-77` として本番で動いています。別仕様書（`work/spec_menu_readonly.md`）の管轄なので、**今回は触らないでください。**利用者へ報告済みです。
