前回の修正を確認しました。942/942 通過、7項目とも入っています。**あなたが報告してくれた仕様書との食い違い6件は、すべて仕様書の側が誤っていました。**仕様書を版 2.16 に直しました。うち1件だけ実装側の追随が要ります。

## 作業：結果画面と選択画面の識別子に店名を残す

`menuReviewStoredIdentity_`（`src/96_Menu.gs:1102`）は「ファイル名＋元行番号＋表示用取引ID」を返しますが、**店名が落ちています**。

`merchantOriginal` は要確認行の O列にあり、取込側が確実に書きます（`70_ImportFlow.gs:98`）。`live` の有無に関わらず使えます。

**なぜ要るか**：`DATE`・`AMOUNT`・`ZERO_AMOUNT` の対象はグループに属さないので、店名を落とすと結果画面（＝取り消せない操作の控え）が「顧客一(C001) / 202601.csv 15 行目」だけになり、**どの店の取引を直したのかが記録に残りません**。§6.5 に理由を明記しました。

**直し方**：`menuReviewStoredIdentity_` の戻り値に店名を含めてください。形は `ｽﾏｲﾙ商店 / 202601.csv 15 行目（TX-2601-0015）`。店名が空または `normalizeMerchant` が空を返すときは `（店名なし）`（既に 584行で同じ判定をしています）。

`EXCLUDE` の選択画面（435行）ではグループ全件が同じ店名になるので冗長ですが、**同じ関数を使って構いません** ── 重複しても害はなく、関数を分けると正本が2つになります。

## 絶対に守ること

- **`clasp push` を実行しないこと。**
- **`src/` の差分は `96_Menu.gs` と `97_Ops.gs` の2ファイルだけ。**
- テストを先に赤にしてから直すこと。既存942件を壊さないこと。
- テストの材料は `runImport` で立てること（§12.3 冒頭）。

## 報告してほしいこと

1. どう直したか（ファイル:行）
2. 赤→緑の確認結果とテスト通過数
3. **仕様書と実際のコードの食い違い**が他にもあれば、直さずに報告してください。前回の6件はすべて本物で、うち2件（識別子・`matchMethod`）は「仕様書が、取込側の書かない値を出せと書いていた」という同じ型の欠陥でした。同類が残っていないか、実データ（`runImport`）で画面を出して確かめてください。
