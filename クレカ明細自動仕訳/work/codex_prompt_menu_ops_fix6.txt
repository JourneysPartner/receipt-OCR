本番投入前の最終確認として、**変異テスト9本**（仕様違反を1つずつ注入して945件を回す）を実施しました。6件は正しく検出されましたが、**3件が素通り**しました。いずれも仕様書が【高】として塞いだ規定です。テストを足してください。**実装は正しいので、直すのはテストだけです。**

仕様書は `work/spec_menu_operations.md`（版 2.16）。

## 素通りした変異1：`correctedDate` の空文字を落とさなくてもテストが緑

注入した変異（`src/96_Menu.gs:856`）：
```
- if (inputs && inputs.correctedDate !== undefined && inputs.correctedDate !== '') input.correctedDate = ...
+ if (inputs && inputs.correctedDate !== undefined) input.correctedDate = ...
```
→ **945/945 緑のまま。**

原因：テスト17 は `correctedAmount: ''` を渡して**金額側**だけを見ています（`Object.hasOwn(seen[0], 'correctedAmount') === false`）。**日付側は無検証**です。

何が起きるか：`AMOUNT` の要確認で担当者が金額だけを直すと `correctedDate: ''` が部品へ渡り、`planned.b` が空文字で書かれて**転記先 B列（利用日）と取引ログ S列が消えます**。§7.4 が最も強く警告した経路の半分です。

**足すテスト**（仕様書 §12.3 テスト17 第2レグの本文どおり）：`AMOUNT` の要確認に**金額だけ**入力（日付の prompt は空のまま）→ M列とW列が変わり、**S列と転記先 B列は不変**。`correctedDate` の鍵が `input` に載らないことも確かめること。

## 素通りした変異2：空店名の `PARTNER` をまとめてもテストが緑

注入した変異（`src/96_Menu.gs:562`）：
```
- (normalized ? 'MERCHANT\u0000' + normalized : 'EMPTY\u0000' + review.reviewId)
+ ('MERCHANT\u0000' + normalized)
```
→ **945/945 緑のまま。**

何が起きるか：店名の無い要確認が顧客ごとに1グループへ潰れ、`ADOPT_EXISTING_PARTNER` を1回押すと**互いに無関係な取引すべての転記先 F列に同じ取引先名が書かれ、辞書にも学習されます**。

仕様書 §7.1 手順2 は「これを固定するテストは無い」と書いていますが、**書けるようになったので足してください。**

**足すテスト**：店名が空（`null`／`''`／全角空白）の `PARTNER` が2件以上あるとき、`buildResolveTargets_` が**それぞれ別の `PARTNER_GROUP`**（`reviews.length === 1`）にすること。実在店名の要確認とは混ざらないことも。

## 素通りした変異3：`unmet` に全件入れてもテストが緑

注入した変異（`src/96_Menu.gs:880`）：
```
- if (result.unmetConditions && result.unmetConditions.length) {
+ if (result.unmetConditions) {
```
→ **945/945 緑のまま。**

原因：テスト14 は `unmetConditions` が**空でない**1件だけを扱うので、どちらの実装でも `unmet.length === 1` になります。

何が起きるか：仕様書 §7.6 冒頭のとおり、**全件確定した押下で画面が「未確定: 3 件」と嘘を表示します**。

**足すテスト**：3件のグループで**2件が確定（`unmetConditions: []`）・1件が未確定**の状況を作り、`outcome.unmet.length === 1` であること、結果本文が「未確定: 1 件」であること。全件確定（3件とも `unmetConditions: []`）の押下では `unmet` が**空配列**で、「未確定」の行が**出ない**ことも。

## 絶対に守ること

- **`clasp push` を実行しないこと。**
- **`src/` を変更しないこと**（実装は正しい。今回はテストだけ）。もし実装を変える必要があると判断したら、変えずに理由を報告してください。
- 足したテストが、**上の変異を注入した状態で実際に赤になる**ことを確かめてください。それが今回の目的です。

## 報告してほしいこと

1. 足したテスト（ファイル:行）
2. **各変異を注入した状態で赤になることの確認結果**
3. テスト通過数
4. 他にも「反転しても緑のまま」の箇所を見つけたら報告してください
